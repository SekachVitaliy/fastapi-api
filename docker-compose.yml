version: "3"

services:
  db:
    image: postgres
    ports:
      - "5432:5432"
    environment:
      - .env
  web:
    build: .
    command: bash -c "uvicorn workshop.app:app --host 0.0.0.0 --port 8000 --reload"
    ports:
      - "8000:8000"
    depends_on:
      - db